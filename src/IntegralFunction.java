public class IntegralFunction {
    private double G;

    public IntegralFunction(double y, double getUpperLimit, double getLowerLimit) {
        double U = y*getUpperLimit + Math.exp(getLowerLimit);

    }
}
